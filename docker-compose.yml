version: "3.9"

services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gaussdevagency_nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/ssl/gaussdev:/etc/ssl/gaussdev:ro
    restart: unless-stopped
